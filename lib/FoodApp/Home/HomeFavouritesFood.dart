import 'package:flutter/material.dart';

class HomeFavouritesFood extends StatelessWidget {
  const HomeFavouritesFood({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
